# 验证记录

日期：2026-09-14。使用 Windows、本机 Python 环境执行。

## 实际依赖

- NumPy 1.26.4
- SciPy 1.13.1
- OpenCV 4.5.5（本机已有版本；requirements.txt 的新安装参考版本为 4.10.0.84，未在本次额外安装该版本）
- Black：本机安装的格式化工具

## 已通过

`python -m unittest discover -s tests -v`：5 项测试通过。

1. DS 模型超过 180° 全视场的有效射线往返；无效像素拒绝。
2. 背靠背双目，240 个空间点，含 40 个错配及角噪声，恢复旋转和平移方向。
3. 独立棋盘格观测拟合内参，并检查没有用于拟合的方向（包含 98° 入射角）的投影误差。
4. 全景覆盖与常量颜色重采样，验证输出尺寸和融合。
5. 两路缺帧时明确拒绝，而不是按排序误配。

合成图像端到端测试：每镜头 16 张棋盘格；3 对有限深度纹理房间图。两个镜头的合成内参相同。实际执行 `run` 的角点检测、内参拟合、SIFT、RANSAC、外参优化和拼接，没有跳过标定。

| 指标 | 结果 |
|---|---:|
| 内参重投影 RMS（cam1） | 0.1170 px |
| 相对旋转与真值差异 | 0.0852° |
| 平移方向与真值夹角 | 0.9724° |
| 几何内点 / 输入匹配 | 1080 / 1080 |
| 调试模式输出全景帧数 | 3 |
| 普通模式输出全景帧数 | 3 |

普通模式检查：输出文件全部是 `panoramas/*.png`；没有中间参数、日志、诊断图。正常运行没有进度输出。普通/调试模式生成的三张全景图逐像素一致。

`python -m black --check src scripts tests`：通过。

## 实际限制

- 合成棋盘格使用已知模型初值，测试不能证明任意真实镜头从默认初值都能收敛。
- 没有真实相机数据，因此没有宣称实拍标定精度或近景接缝质量。
- 本次验证使用公式、合成真值和图像数组检查；本地图片预览工具因 Windows 沙箱启动错误不可用，未做人工视觉验收。
- 当前融合为远景固定映射，合成房间也存在有限深度视差。不能将外参误差与视觉接缝误差混为一谈。
- 平移必须由具有有效视差的数据约束，实际长度来自外部基线配置。

## 复现

在项目根目录运行 README 的合成演示与测试命令。选择未使用过的输出目录。`data/synthetic` 已存在时生成器会拒绝覆盖；无需重新生成即可直接运行。


## src 布局整理验证

- 使用项目内 `.venv` 完成可编辑安装；验证环境通过 `--system-site-packages` 复用现有科学计算依赖，未修改全局安装。
- 脚本与测试不再修改 `sys.path`。包发现限定在 `src/dual_fisheye`，增加 `python -m dual_fisheye` 入口。
- 5 项几何测试通过，Black 格式化完成。
- 从项目目录外启动已安装的模块并拼接 3 帧成功。
- 合成数据集生成逻辑迁入 `datasets.py`，在临时目录生成完整测试数据成功。
